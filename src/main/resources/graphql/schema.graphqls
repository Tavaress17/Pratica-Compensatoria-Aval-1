type Classroom {
    id: ID!
    name: String!
    capacity: String!
    observation: String!
}

type Query {
    classrooms: [Classroom!]!
    classroom(id: ID!): Classroom
}

type Mutation {
    createClassroom(name: String!, capacity: String!, observation: String!): Classroom!
}